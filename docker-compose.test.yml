version: "3.3"

services:
#    backend:
#        environment:
#            EnvironmentName: Test
#        ports:
  #          - 20002:80
    frontend:
        environment:
            EnvironmentName: Test
        ports:
            - 20000:80
            
#postre med anden port?