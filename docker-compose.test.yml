version: "3.3"

services:
    backend:
        environment:
            EnvironmentName: Test
        ports:
            - 61002:80
    frontend:
        environment:
            EnvironmentName: Test
        ports:
            - 61001:80